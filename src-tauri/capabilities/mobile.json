{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "mobile",
  "description": "Capability for iOS (simplified build: no tray, no multi-window settings)",
  "platforms": ["iOS"],
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "cal-sync:default"
  ]
}
